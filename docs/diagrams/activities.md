# 📊 Диаграммы активностей


### 1. Авторизация и регистрация
```mermaid
flowchart TD
    A[Пользователь открывает экран входа или регистрации] --> B{Есть аккаунт}
    B -->|Да| C[Ввод логина и пароля]
    B -->|Нет| D[Ввод имени, email, пароля]

    C --> E{Проверка валидности формы}
    E -->|Валидна| F[POST /api/users/login]
    E -->|Невалидна| G[Показать ошибки ввода] --> C

    D --> H{Проверка валидности формы}
    H -->|Валидна| I[POST /api/users]
    H -->|Невалидна| J[Показать ошибки ввода] --> D

    F --> K{Обработка сервером}
    K -->|Успех| L[200 OK и данные пользователя]
    K -->|Ошибка| M[Показать сообщение об ошибке] --> C

    I --> N{Обработка сервером}
    N -->|Успех| O[201 Created и данные пользователя]
    N -->|Ошибка| P[Показать сообщение об ошибке] --> D

    L --> Q[Сохранить сессию в контексте Auth]
    O --> Q
    Q --> R[Переход на список рецептов]
```

### 2. Загрузка данных
```mermaid
flowchart TD
    A[Пользователь открывает приложение] --> B[Приложение отправляет GET запросы]
    B --> C1[GET /api/recipes]
    B --> C2[GET /api/comments]
    B --> C3[GET /api/ingredients]
    B --> C4[Опционально: GET /api/recipes/:id/users]
    C1 --> D1[Получение списка рецептов]
    C2 --> D2[Получение списка комментариев]
    C3 --> D3[Получение справочника ингредиентов]
    C4 --> D4[Получение пользователей сохранивших рецепт]
    D1 --> E[Отображение данных на главном экране]
    D2 --> E
    D3 --> E
    D4 --> E
    E --> F[Готовность к работе с приложением]
```

### 3. Добавление рецепта
```mermaid
flowchart TD
    A[Пользователь нажимает Добавить рецепт] --> B[Открытие формы]
    B --> C[Ввод названия, длительности PT, ингредиентов с граммовкой]
    C --> D{Проверка валидности данных}
    D -->|Валидны| E{Все ингредиенты существуют}
    D -->|Невалидны| F[Показать ошибки ввода] --> C

    E -->|Да| G[Сформировать DTO рецепта]
    E -->|Нет| H[POST /api/ingredients создать недостающие]
    H --> I{Успех создания}
    I -->|Да| G
    I -->|Ошибка| J[Показать ошибки сервера по ингредиенту] --> C

    G --> K[POST /api/recipes]
    K --> L{Обработка сервером}
    L -->|Успех| M[201 Created]
    L -->|Ошибка| N[Показать ошибки сервера или прав доступа] --> C

    M --> O[Обновить список рецептов GET /api/recipes]
    O --> P[Опционально GET /api/recipes/:id/ingredients]
    P --> Q[Отобразить новый рецепт в списке]
    Q --> R[Уведомление об успешном добавлении]
```

### 4. Удаление рецепта
```mermaid
flowchart TD
    A[Пользователь нажимает Удалить рецепт] --> B{Пользователь владелец или админ}
    B -->|Да| C[Показать диалог подтверждения]
    B -->|Нет| D[Показать отказ в доступе]

    C --> E{Подтверждение}
    E -->|Да| F[DELETE /api/recipes/:id]
    E -->|Нет| G[Отмена и возврат к карточке рецепта]

    F --> H{Обработка сервером}
    H -->|Успех| I[Удалить рецепт из списка]
    H -->|Ошибка| J[Показать сообщение об ошибке]
```

### 5. Редактирование рецепта
```mermaid
flowchart TD
    A[Пользователь нажимает кнопку Редактировать рецепт] --> B{Пользователь владелец или админ}
    B -->|Да| C[Открытие формы редактирования]
    B -->|Нет| D[Показать отказ в доступе]

    C --> E[Редактирование данных рецепта<br/>— название, время, ингредиенты]
    E --> F{Проверка валидности данных}
    F -->|Валидны| G[PUT /api/recipes/:id]
    F -->|Невалидны| H[Показать ошибки формы] --> E

    G --> I{Обработка сервером}
    I -->|Успех| J[Обновить рецепт в списке]
    I -->|Ошибка| K[Показать сообщение об ошибке] --> E

    J --> L[Показать уведомление об успешном обновлении]
```

### 6. Создание ингредиента
```mermaid
flowchart TD
    A[Пользователь открывает раздел ингредиентов] --> B[Нажимает Добавить ингредиент]
    B --> C[Открытие формы добавления]
    C --> D[Ввод данных: название, белки, жиры, углеводы]
    D --> E{Проверка валидности данных}
    E -->|Валидны| F[POST /api/ingredients]
    E -->|Невалидны| G[Показать ошибки ввода] --> D

    F --> H{Обработка сервером}
    H -->|Успех| I[201 Created — ингредиент добавлен в базу]
    H -->|Ошибка| J[Показать сообщение об ошибке] --> D

    I --> K[Обновить список ингредиентов]
    K --> L[Показать уведомление об успешном добавлении]
```

### 7. Сохранение или удаление из избранного
```mermaid
flowchart TD
    A[Пользователь открывает карточку рецепта] --> B{Рецепт в сохранённых}
    B -->|Да| C[Нажать Удалить из сохранённых]
    B -->|Нет| D[Нажать Сохранить рецепт]

    D --> E[POST /api/recipes/:id/users/:userId]
    C --> F[DELETE /api/recipes/:id/users/:userId]

    E --> G{Обработка сервером}
    F --> G

    G -->|Успех| H[Обновить счётчик и состояние иконки]
    G -->|Ошибка| I[Показать сообщение об ошибке]

    H --> J[Опционально GET /api/recipes/:id/users]
    J --> K[Синхронизация интерфейса]
```

### 8. Комментирование
```mermaid
flowchart TD
    A[Пользователь открывает карточку рецепта] --> B[GET /api/comments]
    B --> C[Отобразить актуальные комментарии]

    %% Добавление
    C --> D[Ввод текста комментария]
    D --> E{Текст не пустой}
    E -->|Да| F[POST /api/comments]
    E -->|Нет| G[Отклонить отправку и подсветить поле] --> D

    F --> H{Обработка сервером}
    H -->|Успех| I[Обновить список комментариев]
    H -->|Ошибка| J[Показать сообщение об ошибке]

    %% Редактирование
    C --> K[Выбор комментария для редактирования]
    K --> L{Пользователь автор или админ}
    L -->|Да| M[Изменить текст и отправить PUT /api/comments/:id]
    L -->|Нет| N[Показать отказ в доступе]

    M --> O{Обработка сервером}
    O -->|Успех| P[Обновить список комментариев]
    O -->|Ошибка| Q[Показать сообщение об ошибке]

    %% Удаление
    C --> R[Выбор комментария для удаления]
    R --> S{Пользователь автор или админ}
    S -->|Да| T[Подтверждение удаления]
    S -->|Нет| U[Показать отказ в доступе]

    T --> V[DELETE /api/comments/:id]
    V --> W{Обработка сервером}
    W -->|Успех| X[Убрать комментарий из списка]
    W -->|Ошибка| Y[Показать сообщение об ошибке]
```

