# 📊 Диаграммы активностей

### 1. Загрузка данных
```mermaid
flowchart TD
    A[Пользователь открывает приложение] --> B[Приложение отправляет GET запросы]
    B --> C1[GET /api/recipes]
    B --> C2[GET /api/comments]
    B --> C3[GET /api/ingredients]
    B --> C4[Опционально: GET /api/recipes/:id/users]
    C1 --> D1[Получение списка рецептов]
    C2 --> D2[Получение списка комментариев]
    C3 --> D3[Получение справочника ингредиентов]
    C4 --> D4[Получение пользователей сохранивших рецепт]
    D1 --> E[Отображение данных на главном экране]
    D2 --> E
    D3 --> E
    D4 --> E
    E --> F[Готовность к работе с приложением]
```

### 2. Авторизация и регистрация
```mermaid
flowchart TD
    A[Пользователь открывает экран входа или регистрации] --> B{Есть аккаунт}
    B -->|Да| C[Ввод логина и пароля]
    B -->|Нет| D[Ввод имени, email, пароля]

    C --> E{Проверка валидности формы}
    E -->|Валидна| F[POST /api/users/login]
    E -->|Невалидна| G[Показать ошибки ввода] --> C

    D --> H{Проверка валидности формы}
    H -->|Валидна| I[POST /api/users]
    H -->|Невалидна| J[Показать ошибки ввода] --> D

    F --> K{Обработка сервером}
    K -->|Успех| L[200 OK и данные пользователя]
    K -->|Ошибка| M[Показать сообщение об ошибке] --> C

    I --> N{Обработка сервером}
    N -->|Успех| O[201 Created и данные пользователя]
    N -->|Ошибка| P[Показать сообщение об ошибке] --> D

    L --> Q[Сохранить сессию в контексте Auth]
    O --> Q
    Q --> R[Переход на список рецептов]
```

### 3. Добавление рецепта
```mermaid
flowchart TD
    A[Пользователь нажимает Добавить рецепт] --> B[Открытие формы]
    B --> C[Ввод названия, длительности PT, ингредиентов с граммовкой]
    C --> D{Проверка валидности данных}
    D -->|Валидны| E{Все ингредиенты существуют}
    D -->|Невалидны| F[Показать ошибки ввода] --> C

    E -->|Да| G[Сформировать DTO рецепта]
    E -->|Нет| H[POST /api/ingredients создать недостающие]
    H --> I{Успех создания}
    I -->|Да| G
    I -->|Ошибка| J[Показать ошибки сервера по ингредиенту] --> C

    G --> K[POST /api/recipes]
    K --> L{Обработка сервером}
    L -->|Успех| M[201 Created]
    L -->|Ошибка| N[Показать ошибки сервера или прав доступа] --> C

    M --> O[Обновить список рецептов GET /api/recipes]
    O --> P[Опционально GET /api/recipes/:id/ingredients]
    P --> Q[Отобразить новый рецепт в списке]
    Q --> R[Уведомление об успешном добавлении]
```
